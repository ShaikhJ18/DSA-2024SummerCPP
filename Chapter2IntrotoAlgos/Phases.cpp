#include<bits/stdc++.h>

using namespace std;



int main(){
  int n = 10;
  for (int i = 1; i <= n; i++) {
  // code
  }
  for (int i = 1; i <= n; i++) {
    for (int j = 1; j <= n; j++) {
    // code
    }
  }
  for (int i = 1; i <= n; i++) {
  // code
  }
  /**
  For example, the following code consists of three phases with 
  time complexities O(n), O(n^2) and O(n). Thus, the total time 
  complexity is O(n^2).
  **/
}