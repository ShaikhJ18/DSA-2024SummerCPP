#include<bits/stdc++.h>

using namespace std;


int main(){
  int n = 20;
  //this loop would cause a time complexity of O(n)
  for (int k =0; k <= n; k++){
    cout << k << "\n";
  })
  //these two loops would cause a time complexity of O(n^2)
  for (int i = 0; i <= n; i++){
    for (int j = 0; j <= n; j++){
      
    }
  }
}