#include<bits/stdc++.h>

using namespace std;


int main(){
  int n = 300;
  for (int i = 1; i <= 3*n; i++) {
  // code
  }
  for (int i = 1; i <= n+5; i++) {
  // code
  }
  for (int i = 1; i <= n; i += 2) {
  // code
  }
  //The time complexity of each code is O(n)
  //because we looj at the magnitude of n

  for (int i = 1; i <= n; i++){
    for (int j =i+1; j <= n; j++){
      // code
    }
  }
  //The time complecity of these two loops is O(n^2) still

}